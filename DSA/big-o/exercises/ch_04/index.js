function f(n) {
  let i = 1; // 1
  while (i < n) {
    // 2 for comparison N for Loop
    console.log(`[${i}]`); // Operation for Each Step
    i *= 2; // Divided By Two.
  }
}

// Formula == 2 + n / 2 = Log n.
// Answer is: O(Log n).
